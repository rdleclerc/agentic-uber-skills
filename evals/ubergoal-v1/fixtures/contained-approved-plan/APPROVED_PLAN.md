# Approved plan r3

Scope: change `format_name` to trim surrounding whitespace.

1. Read `formatter.py` and `test_formatter.py`.
2. Change only `format_name`.
3. Run `python3 -m unittest test_formatter.py`.
4. Run one exact-diff review against this plan.

Do not read `archive.md`; do not add artifacts or broad tests.
