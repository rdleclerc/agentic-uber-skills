# Superseded retry coordinator proposal

Before DeliveryLedger existed, this proposal suggested a separate retry
coordinator, retry queue, scheduler registry, and monitoring service. It was
rejected when DeliveryLedger became the canonical owner.
