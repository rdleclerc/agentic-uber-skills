# Rejected cache service proposal

This proposal predates `ReportStore`. It suggested a network cache service,
central key registry, migration framework, and dedicated cache operations team.
It was rejected when cache ownership moved into `ReportStore`.
