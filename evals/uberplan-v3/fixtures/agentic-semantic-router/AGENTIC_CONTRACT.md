# Routing ownership

The deterministic harness may validate envelopes, preserve raw text, and
nominate candidate signals. Ambiguous request-likeness and final user intent
belong to the agent. A message must not be hidden from model policy because it
lacks a keyword.

Routing traces must record the raw message, candidate signals, model decision,
and selected capability without granting the trace or keyword list semantic
authority.
