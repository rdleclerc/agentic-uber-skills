# Wave 3 — Tier-2/3 Independent Adversarial Review

Reviewer: fresh Claude (Fable 5) adversarial lane, no prior involvement in Wave-3 implementation or orchestration. Read-only except this file.
Scope reviewed: pack `agentic-uber-skills` commits `e56574c` (R12+R11 + probe receipt), `c2666f3` (R13), `fea44e5` (R10 pack half + git_ref→origin/main); gaia `agfunder-gaia` commits `21494388` (R10 spine wording), `4a562d25` (R14), `2a156611` (R15) — gaia read from git objects and a dedicated temp worktree at origin/main (`git worktree add --detach`, removed after); the other sessions' checkout was never modified. Authority: `plan-v3.md` §Wave 3 + v3.1 amendments (esp. D, F, G, I), `gaia-child-plan.md`, `wave2-receipt.md` deferrals, `wave3-posture-probe-receipt.md`; conversion precedent from wave1/wave2-adversarial-review.md.
Method: independent rerun of every suite both repos; empirical probes against the new instruments (grader fixtures both ways with true exit codes, packet emission, shape lint on known-bad + real skills, deprecation shim, dispatch preflight, drift strict, freshness-note command semantics, process-compiler degradation with a nonexistent pack root and with real roots, lane-liveness checker strict + report-only); 8 lane-inventory rows re-verified against live plists/state files/logs; heartbeat exit paths traced through `set -euo pipefail` + trap semantics; hermes auth markers grepped in the installed hermes source; both failure INDEXes diffed against their pre-Wave-3 states.

## Verification of claimed green (independent rerun)

- Pack: `pytest tests/ -q` → **62 passed**. Full `lint_pack_contract.py --strict` → PASS (secret scan included). `--drift --strict` → exit 0, **20/20 MATCH**, all gaia entries `source=git_ref(origin/main)`. `--dispatch-preflight .` → PASS (git-dir, git-status, tmpdir). `quick_validate.py` PASS for ubergoal/uberrca/uber-skill-creator/uberskillevolver; per-skill unittest suites PASS. Word budgets: all 10 skills now budgeted (W9 cured); ubergoal **786/800**, uberrca 1374/1500.
- Routing eval, run both ways with unpiped exit codes: pass fixture → `SUMMARY passed=12 failed=0`, **exit 0**; under-tier fixture → FAIL R11+R12 MUST_ESCALATE with tier/artifact/routing/gates reasons, **exit 1**. Grader tests assert both exits.
- Instrument honesty: `evaluate_skill_quality.py` is a 3-line shim → stderr `DEPRECATED: use …lint_skill_shape.py`, **exit 1**. `lint_skill_shape.py` output contains **zero** score/quality-number language (grep-verified); bad-mini-skill fixture → exit 1 with triggering/verification/eval_coverage/package_shape issues, and `test_known_bad_skill_fixture_fails_shape_and_pack_batteries` really does route it through the pack battery (model-pin + word-budget + machine-path all assert). W3 conversion verified real: `test_real_doctrine_drift_registry_is_strict_clean_when_gaia_checkout_exists` + `test_full_strict_lint_fails_seeded_blocking_drift` are in the standing suite; W4's mirror check exists (`test_tier_ladder_registry_patterns_are_mirrored` + diverged fixture).
- Gaia (temp worktree at `2a156611`): `node --test tests/integration/failure_evals.test.mjs` → **2/2 pass**. Pack validator over gaia cases → PASS 10 files; over pack cases → PASS 14; pack `--index` self-check → PASS.
- R10 cutover: ladder Tier-3 row byte-identical ("major refactor / mass deletion") across gaia spine (origin/main), ubergoal, and both registry patterns (drift 20/20 + mirror test). Ledger order correct: gaia canonical 11:39:04, pack pointers/registry 11:40:59, registry updated in the same pack commit per AGENTS.md:24. The commit message's two machinery proofs check out: ubergoal is 786 after the budget gate bounced 802, and `git_ref = "origin/main"` is the right semantics for a repo whose local main another session pins stale.
- R14 inventory truthfulness (8 rows spot-checked read-only against the machine): brain-refresh `finished_at=2026-07-03T18:28:44Z status=ok` exact; slack-live-sync `last_run_at` fresh + `last_sync_at=2026-05-13T02:23:39Z` exactly as the inventory's ambiguity note says; dream-sanitize mtime 13:20Z ↔ "5.4h"; gmail-dealflow-digest 15:47:57Z exact; gaia-mcp err.log 2026-06-14 ↔ "454.8h" exact; qdrant log fresh/log-only; `~/.openclaw/cron/jobs.json` missing exactly as row 17 claims (a real docs-vs-reality catch — home CLAUDE.md still advertises that path); hermes: heartbeat absent, last real report `inbox/hermes-overseer/2026-04-29/report.md`, and the 2026-07-02 attempt's `report.md` is **0 bytes** — "empty reports" verified. The `unknown` classifications are honest gaps under a stated rule ("fresh artifact but no named canary ≠ healthy"), not laziness. All 16 plists on disk appear in the 19-row table.
- `check_lane_liveness.py` (extracted from origin/main, run against the live state root): 7 PASS + hermes-overseer MISSING, `--report-only` exit 0, strict exit 1. Matches dispatch A's claim.
- R15: `bash -n` clean; trap/exit-path trace below; auth markers `codex_auth_missing` / `No Codex credentials stored` exist verbatim in the installed hermes source (`~/.hermes/hermes-agent/hermes_cli/auth.py:2198,2200`) — the shim-based proof used real strings, and an unknown future string degrades to `failed_critique`, never to success. `compile_overseer_process.py` run twice by me: with `--uber-skills-root /nonexistent` → exit 0, every pack section "No data + why", gaia sections still render; with real roots → exit 0, exactly 5 `##` sections, all sub-commands exit 0. Degradation contract genuinely holds.

## Ranked findings

### MAJOR (required conversions before the wave receipt declares acceptance)

**G1. Wave 3 broke the two-layer DB's shared-id status agreement in both directions, and nothing mechanical can see it.**
Evidence: pre-Wave-3 the indexes agreed (`seed`/`seed` for all four ids; verified at pack `88526b5` and gaia `6c48d4c7`). Landed now: pack `evals/failures/INDEX.md:6,17,18` say `seed` for `hermes-overseer-dead-9w`, `op-hang-under-launchd`, `pg-null-upsert-dup` while gaia INDEX (origin/main) says `eval_built` for all three (flipped by 4a562d25/2a156611, pack never synced); inversely gaia INDEX:10 says `seed` for `evaluator-saturation` while pack says `eval_built` (flipped by c2666f3, gaia never synced). Four live divergences, all suites green. Root enabler: the shared validator's `--index` grammar cannot parse the gaia dialect — reproduced: `validate_failure_case.py --index <gaia>/INDEX.md --cases <gaia>/cases` → **exit 1, 10 "malformed index line" errors** (the `date:` segment + repo-prefixed `canonical: agfunder-gaia/evals/failures/`), exactly what BOTH child dispatches flagged. Meanwhile gaia's Node test enforces its own dialect with its own regex, and `compile_overseer_process.py` carries a third INDEX parser. This is Wave-1 F4 recurring (cross-index enforcement missing), now with real drift, and it is a case-9-class weakening of a preserved invariant ("shared ids carry a drift fingerprint" — still absent from the registry).
Why the fix belongs in the pack: `validate_failure_case.py` is the spec-designated **shared** tool (R16a: "shared, invoked by both repos' test suites"), and the gaia dialect is the catalog-schema-correct one for pointer rows (cross-repo canonical + `date:`); the pack's own INDEX uses the same repo-prefixed form for its runtime rows, so the grammar extension serves both.
Minimal fix: (a) sync the four statuses (pack:6,17,18 → `eval_built`; gaia:10 → `eval_built`); (b) extend the shared validator's `--index` grammar to accept optional `date: YYYY-MM-DD` and repo-prefixed canonical dirs, with a gaia-dialect fixture; (c) add a cross-index mode (or aggregator module) asserting shared-id status equality between the two INDEX files, wired into the pack suite with the existing skip-loudly-if-gaia-absent pattern and invoked from the gaia Node test; (d) ride-along: gaia INDEX is missing the `false-green-completion-claims` pointer row entirely (pre-existing since Wave 2, disclosed here, add it in the same sync).

**G2. R11/R12 shipped as doctrine without their promised enforcement artifacts, and the deferral is not yet operator-visible anywhere a receipt lives.**
Evidence: plan R12 test line — "EXEC — dispatch-ledger validator + duplicate-cull fixture; FIX — fake-shape receipt fixture"; R12 body — interface-shape receipt "(case 10, **acceptance-validator-checked**)"; R11 — reproduced-red "referenced in the acceptance artifact — **validator-checked** (case 4)"; amendment G — micro-intent intake "validator named in R10/R11". Landed: `grep -rn reproduced` hits only `references/debug-loop.md`; `validate_acceptance_report.py` and `templates/final-acceptance.md` contain **no** interface-shape or reproduced-red field or check; no ledger validator or duplicate-cull fixture exists (cases 10/11/21 honestly `seed`/pending in their files). `wave3-dispatch1.md:18`'s claim that the reproduced-red receipt is "already validator-defined" is **false** — nothing validator-side exists under any name. The case files and dispatch result are honest (that is the difference from Wave-1 F3), but per-item DoD/test-matrix is unmet and the campaign DoD requires "landed or explicitly re-scoped with operator approval".
Minimal fix (either path): ship the small versions now — one `validate_acceptance_report.py` extension requiring a `reproduced_red` line (or `no_repro_reason`) when the report claims a defect fix, an `interface_shape_receipt` field required when the report mentions fakes/stubs standing in for external interfaces, each with a negative fixture (amendment D pattern), plus a ~40-line dispatch-ledger row validator with a duplicate-cull fixture; **or** record an explicit operator-visible re-scope in the Wave-3 receipt (owner + target wave per artifact), and correct the "already validator-defined" claim wherever the receipt narrates D1. Cases 4/10/11/21 statuses must continue to match whichever path is taken.

### MODERATE

**G3. The behavioral routing eval's two adversarial probes announce their expected answer to the agent under test.** `evals/routing/run_routing_eval.py:113` derives `must_escalate` from the literal marker `EXPECT-ESCALATION` **inside the prompt text**, and `:120-138` renders both the `MUST-ESCALATE` heading and the raw prompt (`EXPECT-ESCALATION: "quick fix" touching provider routing.`) into the fresh-agent packet. Any fresh agent reading the packet is told to escalate, so the two known-bad under-tier probes can never produce a true live failure signal — teach-to-test built into the replacement for the saturated instrument, and a direct contradiction of the pack's own rule (`uber-skill-creator/references/skill-creation-workflow.md`: "do not leak intended fixes, expected answers…"). Grader-side use (fixtures) is unaffected. Minimal fix: strip the `EXPECT-ESCALATION:` prefix and MUST-ESCALATE heading from rendered packets (keep both in the key/grading and in `--grade` output); one fixture asserting the packet for R11 does NOT contain the marker.

**G4. W12's git_ref freshness NOTE went dead the same hour it shipped.** `scripts/lint_pack_contract.py:523-546` runs `git rev-list --count {git_ref}..{git_ref}@{{upstream}}`; after fea44e5 flipped every registry `git_ref` to `origin/main`, that command is `origin/main..origin/main@{upstream}` → **exit 128 "fatal: no such branch: 'origin/main'"** (reproduced), so `git_ref_freshness_note` returns None for every real entry, forever, silently. The suite stays green because `test_doctrine_drift_git_ref_freshness_note_reports_behind_upstream` uses a fixture registry with `git_ref = "main"` and a fake git. V9's "machinery rots" made concrete on the wave's own ride-along. Minimal fix: for remote-tracking refs, report fetch staleness instead (e.g. `.git/FETCH_HEAD` mtime beyond a threshold) or emit the note from `rev-list --count <ref>..<remote-HEAD-after-fetch>` guarded, or remove the note and say so; either way the Wave-3 receipt must not list W12 as delivered-as-specified without this caveat.

**G5. Tier grading is exact-match against prose, so honest live transcripts will false-fail rows R05/R09/R11-style tiers.** `run_routing_eval.py:193` requires `normalize(actual_tier) == normalize(expected_tier)` where expected includes sentences like "Tier 1 plus `loop_mode` if repeated/watch semantics persist" (answer-key rows 5, 9, 11's "Tier 3" is fine). A fresh agent answering `tier: Tier 1 + loop_mode` fails on format, not substance — the instrument will read as routing regression when it is grading brittleness (case-6's inverse failure mode: a gate that says no for the wrong reason). Minimal fix: grade tier by keyword sets like the other fields (or canonical tier tokens `0|1|2|3` + qualifier keywords); keep exact match only for the plain `Tier N` rows.

**G6. R15's cost-telemetry section hardcodes its conclusion and drops the child plan's deterministic cross-check.** `compile_overseer_process.py` (§`section_cost_receipts`) prints a fixed sentence — "Comparison: current receipts now expose minutes and lane_used, but token comparison is still mostly blocked…" — whenever `baseline-cost.md` exists, regardless of data; once tokens start being recorded this becomes a false claim emitted weekly. The child plan's R15.3(b) "deterministic cross-check vs transcript sizes" appears nowhere (wave3-dispatchB.md silently narrowed it). Minimal fix: compute the sentence (tokens-known count already exists in the loop) or label it `static note:`; add the cheap cross-check (receipt `minutes` vs coordination-artifact bytes/mtimes) or record the narrowing as an explicit deferral. Related receipt obligation: with R15 live proof blocked, **amendment I** binds — the orchestrator publishes the first outcome comparison manually from R13c receipts, or the receipt records its deferral with operator approval.

### MINOR

**G7. R14's canonical liveness statement lacks its promised protection and pointers.** wave3-dispatchA.md:5 says the new GAIA_TESTING section "will be drift-fingerprinted" — no registry entry exists; gaia-child-plan.md R14.1 says "spine + CLAUDE.md point" — neither was touched (only `knowledge/gaia-scheduler-ownership.md` gained an inventory pointer). One registry entry (report_only, blocking_wave 4/next) + two pointer lines.

**G8. `check_lane_liveness.py` manifest omits `openclaw-gateway`,** the one healthy inventory lane not covered (dispatch A spec: "seeded from the inventory's healthy lanes"). One `Lane(...)` row (`memory/gateway-canary-state.json`, small TTL).

**G9. "Written on every exit path" is overstated.** `run_hermes_overseer.sh:341` registers the EXIT trap after the preamble and after the `--assemble-prompt` branch (:332 — correctly heartbeat-exempt), so preamble failures (the launchd-env/EX_CONFIG class that killed the lane) and untrapped signals die heartbeat-less; that is only loud once the freshness canary is *scheduled*, which is still an open operator decision (dispatch A: "scheduling decision deferred"). The trap coverage for everything inside `main` is correct (traced under `set -euo pipefail`: export/critique/send failures all set specific outcomes; dry-run and success paths verified in the dispatch's captured JSON). Fix: soften the doc line (`knowledge/hermes-overseer.md:62`) to "every orchestrator exit path after startup", and put the canary-scheduling decision in the wave receipt's operator asks. Note for the receipt: the heartbeat file does **not** exist on this machine today (worktree artifact was destroyed with the worktree) — honest, since no real run has happened; the next scheduled fire (Mon 07-06 08:00 PT) will execute the NEW script in production before the auth due-date (07-10) and should write its first real `blocked_auth` heartbeat; say so explicitly so Monday's outcome is an expected observation, not a surprise.

**G10. `references/dispatch-and-sessions.md:11` fuses two rules under one citation.** "Checkpoint-commit before dispatch." (a real R12 rule, plan + scope.md) is the topic sentence of a paragraph whose body and "This is case 19" describe only the writeability preflight — a reader can conclude checkpoint-committing *is* the preflight. One sentence ("Commit or stash the orchestrator tree before dispatching so implementer receipts reconcile against a known state") separates them.

**G11. Two amendment-G/R13d slivers have no landed home:** "Hermes samples tier0 trailers weekly" (fea44e5 deleted ubergoal's redundant Tier-0 line, which never carried it; it is in no skill, spine, or telemetry section — R15's process.md has no tier0-sampling line) and the learning-promotion "owner: operator; cadence: per-wave now, monthly after" sentence (uberskillevolver has the net-negative rule + Hermes surfacing, not the owner/cadence). One sentence each (uberskillevolver SKILL.md; a later 6th telemetry bullet or receipt note for tier0 sampling).

**G12. Receipt bookkeeping due at wave close:** (a) ubershow slim — Wave-2 receipt named it a Wave-3 small item; not done (still 1,263 words; budget 1400 encodes current size, not the slim) — do or re-defer explicitly; (b) R14's Ranked Gaps were not appended to `knowledge/agent-followups.md` as gaia-child-plan.md §R14.3 requires — either append or declare the inventory's Ranked Gaps section the follow-up register for this campaign; (c) wave3-dispatchA/B packet+result files are untracked (operator decision #4: campaign folder commits with waves); (d) operator decisions #6/#7 live only in the probe receipt — fine, but the wave receipt should cite them as register entries; (e) R10's plan test line ("5 small-task prompts → exactly one artifact; 5 risky prompts still escalate") should be explicitly mapped to the landed 12-row key (which satisfies R13's "≥12 incl. 2 under-tiered" but is not literally 5+5) so the disposition is a recorded convergence, not an unaccounted delta.

## R9b disposition

The probe receipt's before-leg is solid (live PASS, truthful non-certification, confound honestly noted) and case 23's inversion is loader-verified. The proposed re-scope — persona stays in workspace CLAUDE.md because it is not bootstrap-injected; the live gate rides with AGENTS.md — is **legal only as `re_scoped_with_approval`** per `references/operational-states.md:25` ("the operator approved a smaller target before completion; the original outcome remains visible as deferred/not done"): it needs an explicit operator approval line in the register/receipt, not orchestrator discretion — note the probe receipt itself (line 27) actually records the *opposite* disposition (proceed: relocate + repeat probe), so the receipt must pick one and say who approved it. And the re-scope as argued misses residual R9b value: (1) the original R9 motivation was cold-start cost — the persona sections (gaia CLAUDE.md:509-586 Group Chats / Slack Etiquette / Heartbeats) still load into every *coding* session that reads workspace CLAUDE.md, which case 23 made irrelevant to live behavior but not to context spend; (2) a verified two-copy risk: gaia AGENTS.md (152 lines) contains **none** of those persona markers (grep-verified), so live behavior is governed by other bootstrap files while CLAUDE.md carries an unfingerprinted copy that can silently diverge — case-9 class; (3) the probe proved behavior, not provenance — no artifact yet maps each persona rule to the bootstrap file that actually enforces it (SOUL.md/HEARTBEAT.md/etc.); if some rules exist only in CLAUDE.md, live Gaia never reads them (case-2 docs-vs-reality). Recommended shape: approve the re-scope of the *live gate* premise, but keep a Tier-2 residual item — map persona rules to their bootstrap homes, then dedup/pointerize the CLAUDE.md copy (no live gate needed for the CLAUDE.md side; any AGENTS.md/SOUL.md touch takes the gate per the rider) — recorded as the deferred remainder of R9b.

## Failure-catalog intake suggestion

**New case (layer both, canonical process): `two-layer-index-status-drift`** — three Wave-3 commits flipped case statuses on one side of the two-layer DB (pack→gaia for `evaluator-saturation`; gaia→pack for `hermes-overseer-dead-9w`, `op-hang-under-launchd`, `pg-null-upsert-dup`) with every suite green, because the shared validator cannot parse the runtime INDEX dialect and no check compares shared ids across the layers; three independent INDEX parsers now exist. Eval: the G1(c) cross-index assertion, runnable from both suites. **Secondary, `case_updated`: `unverified-baseline-claims`** gains a fourth instance — wave3-dispatch1.md:18 asserted the reproduced-red receipt was "already validator-defined"; one grep falsifies it (G2). Optional third: a body note on `evaluator-saturation` recording G3 (the replacement instrument shipped with its own answer-leak) so the teach-to-test class keeps its history in one place.

## Tier check

Pack dispatches (R11/R12/R13, R10 pack half): behavior surfaces + cross-repo doctrine ⇒ **Tier 2, honored** — orchestrator exact-diff recorded per commit, this review is the independent adversarial lane, scope-fidelity verdict below; no live-injected surface touched (AGENTS.md untouched this wave; drift 20/20 confirms fingerprinted surfaces unchanged except the paired R10 reword). R14 (child A): Tier-2 slice inside the Tier-3 child as the child plan's execution shape allows — **honored**: doc/script/case files only, no plists, checker not scheduled, no launchctl; dispatch honestly flagged the unreachable Claude lane rather than self-approving; the Claude-side review requirement is satisfied by the Fable orchestrator's exact-diff plus this lane. R15 (child B): **Tier 3 obligations partially discharged by design** — plan/exact-diff/adversarial phases done, but note precisely: `scripts/run_hermes_overseer.sh` IS the program the live launchd service executes (plist ProgramArguments → `~/.openclaw/workspace/scripts/run_hermes_overseer.sh`, workspace symlinks to the repo), so merging 2a156611 changed production code for Monday's scheduled fire even though no launchctl ran. That is within the child plan's rollback rules (git-tracked script, additive diff, `bash -n` + shim-proof), but the live-proof leg is pending and the wave receipt must carry it under the production blocker gate, not as done. Classification honored **provided** the receipt records R15 as blocked (below) and nobody claims the child operational.

## Scope echo (vs operator-approved R1–R16 + ride-the-defaults decisions)

Operator approved R1–R16 (scope.md approvals #1–#4) plus two 2026-07-03 defaults: ubershow keep-slimmed (#6) and orchestrator-run posture probe (#7), both recorded in the probe receipt. Wave 3 as landed = R12+R11 (references + routing rows + uberrca boundary + case bindings), R13 in full (rename/shim/shape-lint, routing harness + fixtures + tests, known-bad skill fixture, net-negative promotion validator + fixture, W12/W13 ride-alongs), R10 as the amendment-F residue (ladder wording polish, paired spine/pack commits, registry same-commit, git_ref semantics fix), R14 (doctrine + 19-lane inventory + read-only checker; scheduling explicitly deferred to operator), R15 additive (heartbeat + 5-section telemetry + case/status updates; pipeline restore correctly left to the memory-upgrade session's 04a6615b), and the case-23 gate cure (probe PASS). No smuggled scope found: no Wave-2-class deletions, no launchd/live sends, no vendor-runtime edits, gaia edits confined to dispatch-named files, coordination artifacts within policy (two dispatch files still untracked — G12c). One boundary note: c2666f3 copied `gaia-child-plan.md` into the pack coordination folder (campaign-folder policy #4 covers it). The wave stayed inside R1–R16; the deltas that need receipts are deferrals, not additions (G2, G6, G12).

## Production implementation blocker gate readout (child plan — first campaign use of production mode)

- Production implementation goal? **yes** (R15 touches the live weekly launchd lane's script; R14 explicitly did not).
- Child terminal states the campaign receipt must record:
  - **R14: `operational`** — canonical statement + inventory + checker landed and verified; named residues (canary scheduling decision, G7 fingerprint/pointers, G12b followups) are operator follow-ups, not gaps in its scoped outcome.
  - **R15: `active_blocked`** — NOT hard_blocked and NOT operational. Exact blocker: Hermes Codex auth missing; the one human-owned action is **run `hermes auth` (openai-codex)**, filed, due 2026-07-10 (case file + knowledge/hermes-overseer.md both name it — durable, alert-once, no auto-repair, per case 12's contract). Runnable safe next actions remain, so `hard_blocked_after_safe_action_exhaustion` is not available: (1) orchestrator-run `OVERSEER_DRY_RUN=1 scripts/run_hermes_overseer.sh` against the real workspace (the NO-git constraint was the implementer's, not the orchestrator's) — writes the first real heartbeat; (2) `launchctl kickstart` equivalent proof capturing a production `blocked_auth` heartbeat + exit 1 (safe: send path unreachable while auth fails); (3) rerun `check_lane_liveness.py` after (1)/(2) to close the loop on case 2. Expected-and-fine: Monday 07-06's scheduled fire runs the new code before the auth due-date and should itself produce a `blocked_auth` heartbeat — the receipt should pre-register that expectation (a missing heartbeat Monday = the preamble/EX_CONFIG class instead, G9).
- Parent consequence per `references/operational-states.md:46-49`: with one `active_blocked` child carrying runnable safe actions, **campaign completion is not allowed**; Wave 3 may be accepted as a wave, the campaign stays open pending R15's live leg (+ R9b's approved disposition).
- Campaign-DoD rider the receipt must also record: the first outcome comparison vs `baseline-cost.md` is blocked with R15 ⇒ **amendment I** requires the orchestrator to publish it manually from R13c receipts (or record an operator-approved deferral). The telemetry section that would carry it weekly also needs G6.

## Verdict

The wave's substance is real and verified: both new references encode the campaign's receipted lessons accurately (−C near-miss, stdin probe, exit-codes-not-grep, ancestor check, loader citations all trace to their receipts), the router routes them at 786 words, the saturated evaluator is genuinely dead (shim exits 1; no score anywhere), the routing grader can actually say no, the known-bad skill fails both batteries, the lane inventory survives an 8-row hostile audit against the machine, the heartbeat design is sound where the trap can reach, and every claimed-green suite reproduces. The conversions required are the campaign's own kind of debt: a cross-layer consistency invariant that Wave 3 itself broke while every light stayed green (G1), promised enforcement artifacts that became prose without an operator-visible re-scope (G2), and an instrument pair whose adversarial probes leak their answers and whose freshness note died at birth (G3, G4). None require re-implementation; all are small diffs or receipt lines — and per this campaign's truthful-terminal-state doctrine they belong before the receipt, not after.

Required to convert to accept: (1) G1 status sync + shared-validator dialect fix + cross-index assertion (or, minimally, the sync now and the validator work as an explicit receipted follow-up with owner); (2) G2 ship-or-rescope recorded operator-visibly, including correcting the "already validator-defined" claim; (3) G3 packet leak strip; (4) G4 freshness-note repair or receipt caveat; (5) receipt carries the blocker-gate readout above verbatim in substance (R15 `active_blocked` + safe-action list + amendment-I disposition) and the R9b disposition with an explicit operator approval line. G5–G12 may ride as receipt notes with owners.

WAVE VERDICT: ACCEPT_WITH_FIXES (G1 cross-index sync + shared-validator dialect/cross-check; G2 R11/R12 enforcement ship-or-rescope with the false "already validator-defined" claim corrected; G3 MUST-ESCALATE packet leak; G4 dead freshness note; receipt must record R15 active_blocked + amendment-I comparison and the R9b re-scope as an operator decision)
