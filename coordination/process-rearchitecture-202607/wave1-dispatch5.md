# Wave 1 — Dispatch 5: adversarial-review fixes (pack side)

You are Codex, implementer. Authority: `wave1-adversarial-review.md` findings F3–F9 + intake items (F1 gaia-side is handled by the orchestrator; F2/gaia INDEX by dispatch 6). Work ONLY in this repo; NO git commands. Run full pack tests + per-skill tests for anything touched; print summary with word deltas and FLAG contradictions visibly.

1. **F3 — case-19 preflight probe (Wave-1 EXEC gate).** New aggregator module `--dispatch-preflight [path]` in `scripts/lint_pack_contract.py` (amendment J: module, not CLI): probes (a) git writeability at the given root (`git -C <root> status --porcelain` succeeds AND a `git -C <root> stash create`-class dry write works — use a temp index trick or check .git perms; keep it simple and safe: verify .git dir writable + `git status` exit 0), (b) temp-dir writability (`mktemp` under $TMPDIR + write/delete probe). Exit nonzero with a truthful message on failure. Tests: a fixture-driven test with a read-only temp dir (chmod 555) proving detection; real-machine pass test. Update `evals/failures/cases/dispatch-preflight-writeability.md`: status → eval_built, eval_check names the module.
2. **F4 (pack half) — INDEX consistency enforcement.** Extend `scripts/validate_failure_case.py` with `--index <INDEX.md> --cases <dir>` mode: asserts id-set equality (every case file listed, every listed local-canonical id has a file) and status agreement between INDEX lines and case frontmatter; pointer lines (canonical elsewhere) are checked for id presence only. Wire into pack tests against evals/failures/. Fix pack INDEX.md: remove all "(pending dispatch 4)" markers (dispatch 4 landed in agfunder-gaia; pointer text = "canonical: agfunder-gaia/evals/failures/"); verify the 4 eval_built statuses (claw1-path-rot, install-drift-uberarchitect, doctrine-drift-trio, credential-exposure-by-agent) match case files. Invalid fixtures: id mismatch + status mismatch.
3. **F5 — per-match parameterization exemption.** `is_parameterized_path_line` (lint) + the equivalent in `validate_failure_case.py`: exempt a path match only if THE MATCH lies inside a `${...:-...}` default span (or is tilde-form), not because the line contains one elsewhere. Add the proven-hole probe as a negative fixture ("${X:-~/ok}/docs and also /Users/other/secret" must FAIL).
4. **F6 — blocked_with_failure_intake tightening.** `validate_acceptance_report.py`: when status is `blocked_with_failure_intake`, intake must be `failure_case_id:` or `case_updated:` (NOT `not_applicable_with_reason`). Add fixture pair (blocked-passes, blocked-with-NA-fails) + invalid-status-string test. Optional `--cases-dir` flag: when provided, warn (not fail) if a referenced failure_case_id has no case file. One sentence in uberaccept/SKILL.md where the acceptance report is described: name the three acceptance_status values and that blocked requires a filed/updated case.
5. **F7 — learning-record privacy default.** `uberskillevolver/scripts/new_learning_record.py` default root → `learning/private/local` (gitignored — extend .gitignore if `learning/private/` is not covered... it is per review; verify); `references/cross-machine-learning.md`: raw records under `learning/private/`, sanitized packets copied into `learning/inbox/`. Keep flags working.
6. **F8 — RCA placeholder rejection.** `validate_rca_artifact.py`: surface_enumeration bullets must not normalize into the placeholder set (tbd/todo/none/n/a/unknown/etc. — reuse/extend the existing PLACEHOLDERS). Negative fixture `- tbd`.
7. **F9 — spine lane-policy registry entry.** Add `report_only` drift entry `lane-policy-spine` targeting `${GAIA_ROOT:-~/repos/agfunder-gaia}/knowledge/coding-agent-operating-spine.md`, pattern = read the spine's actual lane-policy sentence (read-only) and fingerprint its stable core (regex ok), severity warn, blocking_wave 2.
8. **F12 (cheap, do now) — set `pending` notes** on the 5 born-diverged registry targets (test-channel-posture gaia targets, canary-command gaia targets): `pending = "Wave 2c propagation"`.
9. **F13 (cheap) — AGENTS.md reword rule line**: add "Reword a fingerprinted rule ⇒ update references/drift-fingerprints.toml in the same commit." near the lane-policy bullet; add to AGENTS_REQUIRED_PHRASES.
10. **Catalog case 22 + case-20 update.** New `evals/failures/cases/parallel-branch-reset-clobbers-merge.md`: layer both, canonical_layer process, date_observed 2026-07-03, what_happened (sanitized): a parallel session's branch-reset workflow dropped an already-merged wave commit from the shared main ref and pushed the loss; zero CI signal because the new tree had no test reference; caught by the Tier-2 adversarial review's ancestor check. failure_class: ref-level lost-update between parallel sessions. eval_check: wave-push protocol asserts each landed SHA is-ancestor of target branch tip before a wave is declared landed (orchestrator protocol) + cross-repo suite wiring makes the tree loud (gaia F2 test). eval_type: checklist. plan_items: R12. status: seed. Update INDEX. Also edit `evals/failures/cases/unverified-baseline-claims.md`: append a second-instance note — dispatch-3 result receipt claimed eval_built=3/case-15-seed while the landed tree had 4 eval_built including case 15 (receipt/diff mismatch caught in wave review).
