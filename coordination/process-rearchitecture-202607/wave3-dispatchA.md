# Wave 3 — Child dispatch A: R14 liveness doctrine + lane inventory (gaia worktree)

You are Codex, implementer, in a DETACHED worktree of agfunder-gaia at origin/main. NO git commands. Authority: `${UBER_SKILLS_ROOT:-~/repos/agentic-uber-skills}/coordination/process-rearchitecture-202607/gaia-child-plan.md` §R14 (read it first). Touchable: `GAIA_TESTING.md`, `knowledge/gaia-scheduler-ownership.md` (pointer only if it exists), NEW `coordination/process-rearchitecture-202607-lane-inventory.md`, NEW `scripts/check_lane_liveness.py`, `evals/failures/cases/{op-hang-under-launchd,pg-null-upsert-dup}.md` + INDEX. Nothing else; NO live service changes (that is dispatch B, orchestrator-run).

1. **Canonical liveness statement** in GAIA_TESTING.md (new section, ≤20 lines): every scheduled/unattended lane ships (a) TTL-bound proof-of-life artifact (state file or DB row with last_success_at + expected cadence), (b) freshness canary failing loudly past threshold, (c) durable human-owned-blocker classification (credential/config drift alerts ONCE with the exact human action, stops auto-repair). Add to the Mandatory Triggers list: "New scheduled/unattended lane ⇒ liveness contract (a)/(b)/(c) before adoption; unattended secret fetches use cache-first + kill-timer (see scripts/run_gmail_pubsub_bridge.sh); any --apply writer ships the run-twice idempotency check (scripts/run_twice_idempotency_check.sh)". Keep wording tight — this section will be drift-fingerprinted.
2. **Lane inventory** at `coordination/process-rearchitecture-202607-lane-inventory.md`: enumerate from FILES ONLY (no launchctl): `~/Library/LaunchAgents/ai.agfunder.*.plist` + `ai.openclaw.*.plist` (read them), `~/.openclaw/cron/jobs.json`. Table: lane | scheduler | cadence | proof-of-life artifact (path; exists? mtime age) | freshness canary (named check? none) | docs-claiming-live (cite doc) | classification (healthy / stale / dead / unknown) + a gaps summary ranked by risk. Known seeds to verify and include: hermes-overseer (dead since 05-04), gmail dealflow digest lanes, brain refresh (state json exists), slack live sync, gmail push freshness check (the good pattern), formal live-proof ledger (stale 761h) + eval cadence (stale 402h) from the posture-probe receipt.
3. **`scripts/check_lane_liveness.py`** (read-only checker, ~100 lines): reads a small lane manifest embedded at top (lane → artifact path + max_age_hours, seeded from the inventory's healthy lanes + hermes heartbeat path `state/hermes-overseer-heartbeat.json` marked expected-after-R15); prints PASS/STALE/MISSING per lane; exits nonzero on any STALE/MISSING unless --report-only. This is the generalized gmail-freshness pattern; do NOT wire it into launchd (dispatch B decides scheduling).
4. **Case updates**: op-hang-under-launchd + pg-null-upsert-dup → status eval_built (their checks are now named mandatory triggers + executable helper); update INDEX accordingly; run the pack validator over cases + --index.
5. Verify: `node --test tests/integration/failure_evals.test.mjs` green; `python3 scripts/check_lane_liveness.py --report-only` runs and prints honestly (hermes will be MISSING/STALE — that is correct output, not failure of your work); print the inventory table inline. FLAG anything ambiguous instead of resolving silently.
