# Wave 2 — Dispatch 7: acceptance-review conversion fixes (pack side; W1/W3/W4/W8/W9/W10/W11)

You are Codex, implementer. Pack repo only; NO git commands. Authority: wave2-adversarial-review.md findings.

1. **W1 — correct the R8 ubershow evaluation** (`coordination/process-rearchitecture-202607/wave2-r8-retirement-evaluations.md`): the "no real artifact" claim is FALSE. Real evidence: `/Users/rob/repos/agfunder-gaia/coordination/gaia-brain-wisdom-event-graph-activegraph/p0-wisdom-grading-board-2026-05-31.html` (`data-artifact-kind="ubershow-decision-board"`) + sibling `p0-wisdom-grading-board-2026-05-31-receipt.md` (operator-registered, `selected_decision: proceed_to_p1_minimal_candidate_design`). Rewrite the ubershow section: corrected evidence list (cite both files, verify read-only), contract verdict (b) flips to pass, recommendation → **keep-slimmed** (name candidate cuts: pattern-kit walkthroughs → references, keep core rule/receipt contract/trigger), add a "correction history" line stating the original sweep searched by directory convention and was falsified by content-marker search (case-20 third instance). Update `evals/failures/cases/unverified-baseline-claims.md` with this third instance.
2. **W3 — standing strict drift gate**: add a pack test that runs `check_doctrine_drift` with `--strict` semantics against the REAL registry (skip-loudly with the env-var name if the gaia checkout is absent); ALSO fold drift + install-sync blocking_failures into the default lint exit path when `--strict` is passed (match the secret-scan handling). Fixture-backed test proving a seeded blocking divergence fails the full `--strict` lint.
3. **W4 — tier-table mirror check**: one test asserting `tier-ladder-table` and `tier-ladder-table-pack` registry patterns are byte-equal (post whitespace-normalization); failing fixture.
4. **W8 — restore the orphaned "100% confident" semantics**: one sentence in `uberaccept/SKILL.md` (near the acceptance-report description): "'100% confident' means scoped confidence after actively trying to disprove the work and finding no material unresolved blocker." Word budget: uberaccept has headroom (1,930/2,150).
5. **W9 — add word budgets**: uberarchitect 900, ubershow 1,400, uber-skill-creator 1,350 in SKILL_WORD_BUDGETS.
6. **W11 — routing-table row in ubergoal** (~15 words): "| Reword a fingerprinted rule | update `references/drift-fingerprints.toml` same commit; drift check gates |". If this pushes ubergoal over its 800 budget, trim elsewhere in the same file (routing table prose, not the fingerprinted tier table) and report the trade. Also add a grading note to `evals/routing/answer-key.md`: rows 4/6's gates require gaia/pack context beyond ubergoal (per W11) — graders accept tier+artifact from ubergoal-only readers.
7. **W10 — receipt notes**: append to `wave2-d2-deletion-receipt.md`: (a) logged policy reversal: uncertain-tier default flipped from lower to higher (anti-down-tiering, case 5, spine-aligned); (b) deferred-skills paragraph deleted from ubergoal; surviving homes ROADMAP.md + uberplan + uberskillevolver.
8. **New failure case pointer (pack side)**: add INDEX.md line for `unverified-context-assembly-assumption` (layer both, canonical runtime — case file lands in gaia separately): pointer format, status seed.

Verify: full pack tests + lint (+ the new strict path), quick_validate for touched skills, budgets green, validate_failure_case (cases + --index; NOTE the new INDEX pointer line must not fail the index check while the gaia-side case file is pending — if the validator requires local files for pointer lines, mark the line per existing pointer convention). Print word deltas + everything. FLAG contradictions.
