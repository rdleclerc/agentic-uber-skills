# Wave 3 — Dispatch 2: R13 instrument replacement (pack)

You are Codex, implementer. Pack repo only; NO git commands. Authority: plan-v3.1 R13 + judgment #8/#18 + wave2 receipt deferrals (W12 freshness note, W13 smoke recording).

1. **Demote the saturated evaluator.** Rename `uber-skill-creator/scripts/evaluate_skill_quality.py` → `lint_skill_shape.py` (keep a 3-line deprecation shim at the old name printing the new path and exiting nonzero so stale invocations fail loudly rather than report scores). Strip "quality score" language from its output (report shape-lint findings only, no /100 scoring); update uber-skill-creator/SKILL.md's Evaluation Mode section: shape lint = hygiene only; QUALITY claims come from behavioral evals + with/without comparisons (compact rewrite, keep the champion/challenger guidance). Update any tests referencing the old name.
2. **Behavioral eval harness (the real instrument).** New `evals/routing/run_routing_eval.py`: reads `evals/routing/answer-key.md` rows; for each prompt emits a ready-to-run fresh-agent packet (the probe prompt + "read ONLY ubergoal/SKILL.md" contract) and, in `--grade` mode, takes a transcript/answers file and grades tier/artifact/routing against the key (exact tier match; artifact + routing by keyword sets per row; gates graded only where the key marks them ubergoal-derivable per the D7 note). It does NOT spawn agents itself (runner-agnostic: Claude Code Agent tool or codex exec both work) — document both invocations in the header. Include the two known-bad under-tiered rows as MUST-ESCALATE. Fixtures: a sample answers file that passes and one that under-tiers and fails. Wire a pack test for the grader logic.
3. **Known-bad skill fixture must fail (case 6 eval_built).** tests/fixtures: a deliberately bad mini-skill (vague trigger, no verification, bloated preamble); test asserts the shape lint flags it AND that it would fail the word-budget/pin/portability battery. Update case evaluator-saturation → status eval_built citing this.
4. **Net-negative learning rule (judgment #18).** uberskillevolver/SKILL.md: promotion contract gains one sentence — "A promoted lesson that adds words to any active skill must name the words removed (net ≤ 0 per skill unless the receipt justifies growth); Hermes surfaces per-skill budget deltas weekly." Update its validator (validate_promotion_batch.py) to require a `words_added/words_removed` pair on promote-now entries; fixture.
5. **W12 (ride-along):** drift report header gains one NOTE line when a git_ref target's local ref is behind its remote tracking ref (cheap: `git rev-list --count ref..ref@{upstream}` guarded, skip silently if no upstream) — freshness visibility, not a gate.
6. **W13 (ride-along):** append to `coordination/process-rearchitecture-202607/wave2-d2-deletion-receipt.md`: the R9a fresh-session orientation smoke was verified by the Wave-2 acceptance reviewer by direct read (1-hop reachability) — recorded as the formal smoke record.

Verify: full pack tests + lint --strict; the grader fixtures both ways; per-skill checks for uber-skill-creator + uberskillevolver; word deltas (budgets hold). Print everything; FLAG contradictions.
