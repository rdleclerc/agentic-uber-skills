# Wave 2 — Dispatch 2: R6 thin ubergoal + tiered plan templates + routing answer key

You are Codex, implementer. Pack repo only; NO git commands. This is the campaign's most delicate dispatch: you are rewriting the router. P1 rule is absolute: NO invariant may be deleted without a named new home recorded in the deletion receipt. Read first: ubergoal/SKILL.md (current, 2,767 words), the spine's new Review ladder (${GAIA_ROOT:-~/repos/agfunder-gaia}/knowledge/coding-agent-operating-spine.md — read via `git -C ${GAIA_ROOT:-~/repos/agfunder-gaia} show main:knowledge/coding-agent-operating-spine.md` because the checkout is on another branch), references/drift-fingerprints.toml (tier-ladder-table pattern — your condensed table must match it EXACTLY under whitespace normalization), plan-v3.md R6 + amendments E/F/G, uberplan/SKILL.md (receiving TUR), templates/plan-contract.md.

## 1. Rewrite ubergoal/SKILL.md → ≤800 words

Keep (tightened): frontmatter description (unchanged — trigger contract is not in scope); core rule ¶ (thin wrapper + bounded-loop contract, ≤80 words); routing table (all current rows incl. loop_mode/campaign/refactor/tier3-proof rows, as table rows with pointers); **the condensed tier table = EXACTLY the 4-row ladder table from the registry pattern** + one rider line: "Riders + audit rule: spine §Review ladder is canonical; receipts record tier + one-line justification; reviewer's first check is tier correctness. If uncertain, take the higher tier."; **micro-intent fast path section (KEEP — compress to ≤90 words: the 4-element artifact + when it suffices + the intake field per amendment G: micro-intent artifacts carry `failure_case_id | case_updated | not_applicable_with_reason`)**; scope-artifact gate (≤40 words); completion rule (≤70 words: uberaccept says done + intake filed + every repo committed/reverted/approved-dirty + git status in handoff); Tier-0 boundary line (amendment G: "Tier 0 = typo/cosmetic only; anything fixing observed misbehavior is ≥ Tier 1; tier0: commit trailer"); Helpful resources pointer list.

Delete → receipt (each with named home): Task Understanding Review 7-question block → uberplan owns it (add a compact "Task Understanding Review" section to uberplan/SKILL.md ONLY if not already covering it — it has a copy; keep uberplan's, delete ubergoal's; routing table row points "misunderstanding-prevention review → $uberplan"); thread-cap topology + parent/child blocked-state machine + operational parent/child section → NEW `references/operational-states.md` (single home; write it complete: standard/deep/wide presets + approval rules, blocked-state taxonomy incl. active_blocked/hard_blocked_after_safe_action_exhaustion, parent-completion conditions, per-child terminal states — source the content from ubergoal's current text; uberplan/uberaccept/uberskillevolver copies are deleted in R5, so this reference must be self-sufficient); the inlined Claude-adversary block → keep ONLY the 3 ubergoal-specific questions + one line "Contract: references/claude-adversary.md (subprocess reference-following proven — coordination/process-rearchitecture-202607/wave2-v3-probe.md)"; implementation-effort table → fold its signal into one line in the tier table's rider ("effort scales with tier; xhigh only where a smaller slice cannot preserve safety"); goal-objective detail → pointer to references/goal-objective.md + validator line; architecture-stepback route → already a routing-table row, delete the prose section; Coding Agent Work Contract section → one routing-table row + keep the parameterized paths.

Deletion receipt: `coordination/process-rearchitecture-202607/wave2-d2-deletion-receipt.md` — table: removed block (first ~6 words + word count) | invariant it carried | new home (file + anchor) | verified-present-there yes/no.

## 2. Tiered plan templates

- `uberplan/templates/plan-tier1.md` (NEW, ~10 sections): Objective; Operator-original ask (verbatim) + scope echo; Acceptance criteria (checkable); Out of scope; Proof plan (commands/fixtures); Risks + rollback; Verification results; Failure intake field; Receipts/links; Open questions. Keep it ≤60 lines with terse inline guidance.
- Rename `uberplan/templates/plan-contract.md` → `uberplan/templates/plan-tier3.md` (git-level rename is the orchestrator's job — you create plan-tier3.md as a copy and replace plan-contract.md content with a 3-line tombstone pointer "moved to plan-tier3.md; Tier 1 uses plan-tier1.md"; orchestrator will finalize).
- `uberplan/scripts/validate_plan_contract.py`: add `--tier tier1|tier3` (default tier3 = current behavior for back-compat); tier1 mode validates the 10-section shape + non-empty operator-ask + intake field. Fixtures both ways (valid tier1 passes; tier1 missing operator-ask fails). Update uberplan/SKILL.md's template references minimally (name both templates + when each applies, ≤3 lines changed; uberplan's full rewrite is R5/R8 scope, not yours).

## 3. Routing answer key

`evals/routing/answer-key.md`: the 10 prompts from plan-v3 R6 with expected routing (tier + artifact + gates), formatted one row each: prompt | expected tier | expected artifact | expected gate(s). Add 2 known-bad under-tiered rows marked EXPECT-ESCALATION (a "quick fix" touching provider routing; a "tiny doc edit" to workspace CLAUDE.md persona content → live-injected rider). This file is the fixture for R13's fresh-agent evals; no harness yet.

## 4. Standing word budget

Add to `scripts/lint_pack_contract.py` a `SKILL_WORD_BUDGETS` check: ubergoal/SKILL.md ≤ 800 words (body incl. frontmatter as counted by `wc -w`). Other skills: no budget yet (land with R5/R8 receipts). Failing fixture.

## 5. Verify + report

Pack tests + lint green; drift report: tier-ladder-table must now MATCH on ubergoal/SKILL.md; ubergoal word count reported (target ≤800); print the deletion receipt inline in your summary; run `python3 ubergoal/scripts/validate_goal_objective.py --help` and ubergoal tests to confirm nothing references deleted sections. FLAG anything you could not preserve losslessly.
