# Wave 2 — Tier-2 Independent Adversarial Review

Reviewer: fresh Claude (Fable 5) adversarial lane, no prior involvement in Wave-2 implementation or orchestration. Read-only except this file.
Scope reviewed: pack `agentic-uber-skills` `9f400f9..8c249b3` (161ec43 / fb61d32 / fe0268b / f2f922c / 8c249b3); gaia `agfunder-gaia` commits `31ea231c` + `5ae8cbd1` (read from git objects; the working checkout is another session's branch `feat/e6-auto-post-updates` and was not modified); guide `agentic-architecture-guide` commit `90a0ddd`. Authority: `plan-v3.md` Wave 2 (R7/R6/R5/R3abc/R8/R9a; R9b live-gated) + round-2 amendments E/F/G/H + the Wave-1 conversion-criteria precedent.
Method: independent rerun of every suite/lint/validator; byte-level fingerprint verification; empirical reword probe against the drift checker; full diff read of the old ubergoal vs receipt rows (orphan hunt); ≥9 deletion-receipt rows re-verified at their claimed homes; R8 evidence claims re-swept in gaia coordination by content marker, not filename convention; OpenClaw context-assembly checked in the installed runtime (`dist` source), not assumed.

## Verification of claimed green (independent rerun)

- Pack: `pytest tests/ -q` → 54 passed. Per-skill: ubergoal 9, uberplan 43, uberaccept 34, uberassess 9, uberrca 5, uberskillevolver 10, ubersimplify 12, uberarchitect 3, ubershow 6(+14 subtests), uber-skill-creator 10 — all pass. `quick_validate.py` → all 10 valid. Default lint, `--install-sync --strict`, `--secret-scan --strict` → exit 0.
- Drift: `--drift --strict` → exit 0, 19 MATCH, 0 DIVERGED, 12 registry entries, git_ref(main) used for all 7 gaia-side entries. MATCH lines are non-vacuous: the tier-ladder table is byte-identical (post whitespace-normalization) across the two registry patterns, `ubergoal/SKILL.md#review-ladder`, and gaia `main:knowledge/coding-agent-operating-spine.md` — verified by direct extraction and comparison, independent of the registry.
- Reword-catch probe (empirical): a one-word reword of the precedence sentence ("is the acceptance review" → "performs the acceptance review") against a blocking entry → `--drift --strict` exits 1 with DIVERGED. The blocking entries do catch rewording in focused strict mode (but see W3).
- Word counts (recomputed): pack SKILL.md total **14,129** (`wc -w */SKILL.md`), matching the claimed ~14.1k. Chain from baseline is arithmetically consistent: 19,176 → 14,840 (post-D3, per commit) → −425 (D4) → −286 (D6) = 14,129. All 7 configured budgets respected (ubergoal 786 ≤ 800).
- Gaia: `31ea231c` and `5ae8cbd1` are both ancestors of gaia `main` AND contained in `origin/main` (pushed) — the Wave-1 F1 clobber class did not recur. `node --test tests/integration/failure_evals.test.mjs` → 2/2 pass (the test file is identical between the working tree and `main`, so the run is valid for main); `check_coding_agent_spine.py --json` → 5/5 pass. Newer commits on origin/main beyond local main touch no doctrine files.
- Guide: `90a0ddd` lands 5,034 → 1,312 words (recounted), frontmatter description unchanged, repo in sync with origin.
- V3 entry gate: `wave2-v3-probe.md` records a real subprocess sentinel PASS; ubergoal:60 cites it; the deleted adversary blocks each left a pointer + retained unique questions, satisfying the gate's contingency design.
- Wave-1 fix ride-alongs spot-checked: AGENTS.md:24 reword rule (F13) present; `lane-policy-spine` registry entry (F9) present and MATCHing; secret-scan gains `github_pat`/`xoxc`/`xoxs`/`xapp` + commit-hex exemptions (F10); D6 hardened `validate_failure_case.py` (F11) and pattern expansion uses defaults not env (tested in suite).

## Deletion losslessness (sampled ≥9 rows across d2/d3/d4/d6 + gaia D5)

Verified present at the named home AND discoverable from the deletion site or routing table: TUR seven questions + ambiguity gate (d2→`uberplan/SKILL.md:26-49`, routed from ubergoal:18); topology presets, per-child terminal states, blocker taxonomy, safe-work exhaustion, parent completion (d2/d3→`references/operational-states.md`, routed from ubergoal:31, pointed from uberplan:72,152 and uberaccept:40); adversary contract incl. cross-model symmetry, prompt-save rule, distinct-causal-layers, `No material impact` = non-evidence, staleness (d2/d3→`references/claude-adversary.md:16,27,92,106,110`, pointer retained in all five skills); loop acceptance lens complete (d3→`references/loop-engineering.md:54-66`, pointed from uberaccept:32); evolver fossils (d4→`evals/failures/cases/scope-laundering-20260528.md`, `false-green-completion-claims.md` + INDEX row + `uberskillevolver/references/regression-lessons.md`, pointed from SKILL.md:116,153); ubersimplify gates/test-confidence (d6→`ubersimplify/references/`, active pointers at SKILL.md:41,49 with the key invariants still inline); gaia affordance-proof (D5→guide `docs/00-agentic-change-protocol.md`, pointed from CLAUDE.md, AGENTS.md, INIT.md Ownership Pointers); mandatory triggers (D5→GAIA_TESTING.md:25-31, registry MATCH).

Orphan hunt over the full old ubergoal (2,767 words): **one genuine orphan found** (W8); one policy reversal not logged (W10); one deletion missing from the receipt but with surviving homes (W10). Everything else is receipt-mapped or condensed with the invariant intact.

## Router regression (786-word ubergoal)

Own probes against the text alone: skill-prompt edit → ladder row 2 "behavior surfaces (prompts/skills/evals)" = Tier 2, decidable; cross-repo doc-pointer fix → row 2 "cross-repo doctrine/pointer edits" = Tier 2, decidable; "keep rerunning until green" → routing row `loop_mode` + loop-engineering + uberrca-on-repeats, decidable; "small fix touching data-subject routing as micro-intent" → micro-intent guard names data-subject surfaces as disqualifying = escalate, decidable. The tier table + riders + micro-intent guard carry the load; the fast path per amendment F is present with the hard de-escalation guard. Gaps are W7/W8/W11.

## Ranked findings

### MAJOR (required fixes before wave acceptance)

**W1. The R8 ubershow evaluation's load-bearing evidence claim is false — a real ubershow decision board WITH sibling receipt exists in gaia coordination history.**
Evidence: `/Users/rob/repos/agfunder-gaia/coordination/gaia-brain-wisdom-event-graph-activegraph/p0-wisdom-grading-board-2026-05-31.html:421` — `<main data-artifact-kind="ubershow-decision-board">` — plus sibling `p0-wisdom-grading-board-2026-05-31-receipt.md` following the full receipt convention (artifact path, decision question, default option, sources, view-not-authority constraints, `registration_status: registered_from_user_paste_2026-05-31`, `selected_decision: proceed_to_p1_minimal_candidate_design`, next_action). This was a real, operator-registered decision board that drove P1 scoping. `wave2-r8-retirement-evaluations.md:52-54,63` claims "No files under `coordination/**/ubershow/`" and "no sibling Markdown receipt … was found" — the sweep searched by directory/filename convention and missed content-marked artifacts.
Why it matters: contract requirement (b) "real triggering task class observed in coordination history" PASSES on corrected evidence, flipping the recommendation from archive to keep(-slimmed) under the evaluation's own contract. The operator archive decision at Wave-2 acceptance would otherwise be made on false evidence — catalog case-20 class. Mitigation: nothing executed; ubershow remains installed in both roots (install-sync green), so the error is confined to the recommendation artifact.
Minimal fix: correct the ubershow section (evidence list, contract verdict, recommendation) before the operator decides; re-run the sweep by content marker (`data-artifact-kind`) and `*-receipt.md` siblings; file `case_updated` intake (see below). Note the honest parts stand: the operator-decision framing, the stay-of-execution option, and the ubersimplify/uberarchitect verdicts (both re-verified: the 2026-05-11 ubersimplify audit trail and both stepback packets exist).

**W2. Gaia `AGENTS.md` is verifiably live-injected into OpenClaw session context; Wave 2 edited it twice at Tier 2 with no live gate — violating the rider the same wave landed. The finding also inverts R9b's premise.**
Evidence: `~/.local/lib/node_modules/openclaw/dist/agents-DtfX8YSM.js` — `BOOTSTRAP_FILE_NAMES = [AGENTS.md, SOUL.md, TOOLS.md, IDENTITY.md, USER.md, HEARTBEAT.md, BOOTSTRAP.md]`; `dist/run-attempt-BXh5Tiph.js:559` — "OpenClaw loaded these user-editable workspace files for the current turn. Codex loads AGENTS.md natively…"; `~/.openclaw/workspace → /Users/rob/repos/agfunder-gaia`. Workspace `CLAUDE.md` is NOT in the bootstrap list and `openclaw.json` injects no CLAUDE.md/INIT.md. Wave-2 edits to the injected file: `31ea231c` (review-gate paragraph → ladder pointer + precedence sentence) and `5ae8cbd1` (affordance-proof block −388w, Slack-proof restatement −133w). The rider landed in `31ea231c` itself: "any surface injected into live OpenClaw session context additionally takes the GAIA_TESTING live-proof gate (those are runtime behavior changes, not doc edits)."
Why it matters: live Gaia's session context materially changed this wave, twice, with no live probe and no context-assembly citation — while the campaign guarded CLAUDE.md (not injected per core+config evidence) as the dangerous file. This is a plan-model error surfaced by verification, not implementer negligence: plan-v3 explicitly tiered R7/R5/R9a at Tier 2. Practical risk is bounded (deleted content is coding-agent doctrine replaced by pointers, and the V3 probe proved subprocess pointer-following), but the wave's own law says live proof.
Minimal fix: record the `BOOTSTRAP_FILE_NAMES` citation in the wave receipt; run one pre-approved live probe in `#gaia-testing-alpha` (same prompt pre/post is no longer possible, so a posture-check probe + explicit operator waiver of the before-leg is the honest shape); feed the corrected injection model into R9b (precondition (a) is now half-answered, and the R9b probe should first test whether CLAUDE.md edits affect live behavior at all).

**W3. Blocking drift entries gate nothing automatically: no standing surface runs `--drift --strict` against the real registry, and the default lint drops drift/install-sync blocking failures even with `--strict`.**
Evidence: `scripts/lint_pack_contract.py:854-864` — the default path folds only `report.errors` (registry-load errors); under `--strict` it special-cases only `secret_report.blocking_failures`; drift and install-sync `blocking_failures` never reach the default exit code. `tests/test_pack_contract.py:176-223` exercises `--drift --strict` only against fixture registries. Proven empirically: with a seeded blocking divergence, focused `--drift --strict` exits 1, but the full lint with `--strict` and the same divergent registry prints "PASS: pack contract lint passed".
Why it matters: R5's test line says "EXEC drift check (now blocking)"; Wave-1 F15 flagged this exact gap; Wave 2 flipped `adoption_state` to blocking without wiring any automated strict run. A fingerprinted rule can now diverge on gaia main and every standing suite stays green until someone manually runs the focused mode — V9's "checks bypassed as noise" made concrete.
Minimal fix: one pack test running `--drift --strict` against the real registry (skip-loudly if the gaia checkout is absent), and/or fold drift+install-sync blocking failures into the default-path errors under `--strict`, matching the secret-scan handling.

### MODERATE

**W4. Amendment E's tier-table protection is two independent registry entries, not a cross-check.** `references/drift-fingerprints.toml:168-206` — `tier-ladder-table` (canonical=spine) and `tier-ladder-table-pack` (canonical=ubergoal) carry byte-identical patterns today (verified), but nothing asserts they stay equal, and `check_doctrine_drift` (lint_pack_contract.py:520-581) has no pairing logic. An agent rewording the pack table and updating `tier-ladder-table-pack` in the same commit — exactly what AGENTS.md:24 instructs — passes drift while silently diverging from the spine, defeating "fingerprinted IN FULL against the spine ladder". Fix: a `mirror_of` registry field or one test asserting the two patterns are byte-equal.

**W5. The committed gaia W2-D5 receipt contradicts its own commit on the R3a stub.** `coordination/process-rearchitecture-202607-gaia-w2d5-receipt.md` (committed in `5ae8cbd1`, never updated) says "The requested stub is blocked pending orchestrator decision", shows `AGENTIC_ARCHITECTURE.md` 11,657→11,657 delta 0, and "R3a stub blocked, full guide still carries Version: 1.5" — while the same commit ships the 14-line stub (−11,930 lines) and repoints `knowledge/gaia-confidentiality-doctrine.md:439`. The largest single deletion of the campaign is documented by a receipt claiming it didn't happen (Wave-1 F4 receipt/diff-mismatch class repeating). The stub itself is sound: startup-canary contract preserved (stub rule 1 + retained CLAUDE.md canary paragraph), consumer table correct, confidentiality repoint verified. Fix: one gaia commit updating the receipt (stub landed via orchestrator blocker-resolution + corrected line counts), or an errata block in the wave receipt.

**W6. R9b's proposed terminal classification `blocked_other_session_wip` is not a valid state and misidentifies the blocker.** `references/operational-states.md:19-38` allows `operational | blocked | re_scoped_with_approval` with the blocked taxonomy `active_blocked | hard_blocked_after_safe_action_exhaustion`; no landed artifact defines `blocked_other_session_wip`. Factually, the gaia checkout being another session's branch did not block D5 (it ran in a dedicated worktree per `wave2-dispatch5.md`) and cannot hard-block R9b. R9b has runnable safe next actions today: the context-assembly citation (W2 supplies most of it), the "before" Slack-behavior probe capture, and the prepared single-commit move. Correct classification: `active_blocked` (wave stays open on R9b) or operator-approved `re_scoped_with_approval` deferring it out of Wave 2 — either way with the safe-action list and unblock owner recorded.

**W7. R6's fresh-agent routing smoke was under-executed and over-claimed.** Plan R6: "each prompt routed by a fresh subagent reading only the new ubergoal; grade vs key" (10 prompts; landed key has 12 rows). `wave2-d2-deletion-receipt.md:19` claims "5/5 vs answer key, including both adversarial cases (provider-routing…; repeated-timeout symptom-patching → uberarchitect…)" — but "repeated-timeout symptom-patching" is not an answer-key row (`evals/routing/answer-key.md` has no uberarchitect row), and the key's second EXPECT-ESCALATION row (live-injected persona edit) is unaccounted. At most 4 of 12 key rows were smoke-tested; "5/5 vs answer key" overstates. Fix: run the remaining rows (cheap) or record an explicit partial-smoke re-scope in the wave receipt; add an uberarchitect row to the key if it is meant to be graded.

**W8. One genuinely orphaned rule from the ubergoal thinning: the "100% confident" semantics.** Old ubergoal (9f400f9:129): "For '100% confident,' mean scoped confidence after trying to disprove the work and finding no material unresolved blocker." Not in any d2 receipt row; absent from ubergoal, uberaccept, and all references (grep-verified) — only a bare template field survives (`uberaccept/templates/final-acceptance.md:278` "100% confident within scope? yes/no"). Meanwhile `ubergoal/SKILL.md:3` still advertises "100% confident strategy checks" as a trigger, so a routed trigger now has no semantic definition anywhere. Fix: one sentence in uberaccept/SKILL.md (or ubergoal's Completion Rule).

### MINOR

**W9. Word budgets missing for 3 of 10 skills.** `lint_pack_contract.py:32-40` covers 7; uberarchitect (818), ubershow (1,263), uber-skill-creator (1,215) have none, vs judgment #14 "each other skill: its post-Wave-2 count +10%". ubershow was defensible pending the archive decision (now reopened by W1); uberarchitect's keep verdict makes its budget due (900). Fix: add 3 entries.

**W10. Two receipt-completeness gaps in d2 (no content loss).** (a) Un-logged policy reversal: old "If uncertain, choose the lower tier unless concrete risk requires escalation" → new ubergoal:42/spine "If uncertain, take the higher" — correct direction (anti-down-tiering, case 5) and spine-aligned, but a reversal the receipt should note, not silently flip. (b) The deferred-skills paragraph (ubercode/ubergit/ubereval/uberui + the ubertesting ban) was deleted without a receipt row; it survives at ROADMAP.md:31,72-75, uberplan/SKILL.md:138, uberskillevolver/SKILL.md:96. Fix: two lines in the wave receipt.

**W11. Two answer-key gates are not derivable from ubergoal alone.** Row 6 expects "drift-registry update" — that rule lives only in pack AGENTS.md:24 (ubergoal and the spine ladder never mention the registry); row 4 expects "canary per GAIA_TESTING when Gaia surface" (gaia-context knowledge). A fresh subagent reading only ubergoal (the smoke contract) cannot emit those gates. Fix: one routing-table row for fingerprinted-rule rewording (~15 words fits the 800 budget), or mark those gates as pack-context in the key's grading notes.

**W12. git_ref drift mode reads the LOCAL `main` ref; a stale local main can mask upstream doctrine drift until fetched.** Observed live: local gaia main (5ae8cbd1) is behind origin/main (ddfa7889) — no doctrine files touched, harmless today, but worth a registry-header note or a freshness NOTE line in the report. (Conversely, git_ref mode is what made the drift check correct while the gaia working tree sits on another session's branch — verified working as designed.)

**W13. R9a's fresh-session orientation smoke is structurally satisfied but unrecorded.** INIT.md's Ownership Pointers reach spine/ladder, GAIA_TESTING, coordination, and the affordance protocol in 1 hop (verified by read), but no artifact records the plan-required smoke actually running. Fix: run once, one line in the wave receipt.

## Word-count arithmetic honesty

Baseline 19,176 (corrected, exact). Landed: **14,129** (independently recomputed) — receipt-consistent across d3/d4/d6 deltas. Conditional target: with the W1 correction the ubershow retirement evaluation FAILS on its own contract, so the applicable branch is the else-branch **≤14,500 — met**. For completeness: archiving ubershow (1,263) would give **12,866** installed-skill words, still 866 over the ≤12,000 branch — i.e., even the archive path never reached 12k; the wave receipt should state this plainly rather than implying the archive decision closes the target. DoD explicitly allows missing the number with receipts; the per-skill delta arithmetic exists across the three deletion receipts and should be consolidated into the wave receipt.

## Failure-catalog intake suggestion

**`case_updated`: `unverified-baseline-claims` gains two instances.** (1) The R8 ubershow sweep asserted a load-bearing negative ("no board + sibling receipt exists") that a content-marker search falsifies in one grep (W1) — third instance of unspot-checked load-bearing claims. (2) The gaia W2-D5 receipt shipped inside the commit that contradicts it (W5) — same family as the Wave-1 dispatch3 receipt/diff mismatch. **Recommended new case (layer: both, canonical gaia): `unverified-context-assembly-assumption`** — Wave 2's tier/risk model assumed workspace CLAUDE.md is live-injected and AGENTS.md edits are doc-safe; the installed runtime shows the inverse (W2). Eval: any receipt classifying a surface as live-injected or not must cite the loader (config/code path, e.g. `BOOTSTRAP_FILE_NAMES`), validator-checkable as a required field on gaia doctrine-surface receipts.

## Tier check

**Tier 2 is correct for Wave 2 as landed, per the plan as approved,** with one rider exception to cure. Cross-repo doctrine/pointer edits ⇒ Tier 2 minimum; this review constitutes the required independent adversarial lane; orchestrator exact-diff reviews are recorded per dispatch; implementers flagged unreachable Claude lanes rather than self-approving (no-self-approval invariant held). The exception: gaia AGENTS.md edits touched a verified live-injected surface, which per the wave's own landed rider takes the GAIA_TESTING live-proof gate (W2) — cure with the probe + citation or an explicit operator waiver in the wave receipt. R8 correctly withheld archive execution for operator decision; amendment H's posture change landed as the operator-approved direction with the veto point visible.

## Scope echo (vs operator-approved R1–R16)

Operator approved implementation of R1–R16 after two review rounds. Wave 2 as landed = R7 (spine ladder + precedence, both fingerprinted), R6 (thin ubergoal 2,767→786 + tier1/tier3 templates + answer key), R5 (pack dedup: adversary 5→1, state machine 4→1, loop summaries, Gall's Law; gaia dedup: ladder/test-triggers/coordination-roles/memory-split/affordance-proof), R3a (stub, blocker resolved via confidentiality repoint), R3c (channel/canary unification per amendment H), R3 hardening (git_ref mode, registry blocking flips — D6), R8 (evaluations only; no archives executed), R9a (INIT single unconditional read + CLAUDE coding-half slim; persona untouched — verified on the diff), plus Wave-1 fix ride-alongs (F10/F11) in D6. The Wave-2 entry ordering held (R7/R6 landed before D3 dedup, per commit timestamps). The micro-intent fast path in ubergoal is amendment-F-sanctioned R6 content, not smuggled R10. Gaia edits stayed within the 7 dispatch-named files + receipt. R9b: not started — needs W6's honest classification. No unapproved scope found; one scope shortfall: home `/Users/rob/CLAUDE.md:31-34` still carries the pre-R7 binary 4-phase rule (R5's opus-lane 5→1 is 4/5 complete; the home copy contradicts the ladder for Tier 0/1 work, though it does point at the spine as canonical at line 40-41) — either repoint it this wave or record it as an explicit carry-over in the wave receipt with a fingerprint so it cannot rot silently.

## Verdict

The landed trees are in materially better shape than Wave 1's: both gaia commits are ancestor-verified and pushed, deletion receipts are real and (sampled at 9+ rows) lossless with discoverable pointers, the thin router routes, the fingerprint set is non-vacuous and byte-consistent across all three tier-table homes, and every suite reruns green independently. The blockers-in-waiting are all documentation/gating truthfulness, not lost content: a false evidence claim feeding a pending operator decision (W1), the wave's own live-injection rider unmet on a verified-injected file (W2), and a drift gate that only fires when invoked by hand (W3). None require re-implementation; all are cheap, and none is irreversible yet — which is exactly why they must be fixed before the wave receipt declares acceptance.

Required to convert to accept: (1) correct the R8 ubershow evaluation before the operator archive decision + file the intake (W1); (2) record the OpenClaw injection citation and run the pre-approved live posture probe or obtain an explicit operator waiver for the AGENTS.md edits (W2); (3) wire `--drift --strict` (real registry) into a standing suite or fold blocking failures into the default lint (W3); (4) fix the self-contradicting gaia W2-D5 receipt (W5); (5) classify R9b with a legal state + runnable unblock list (W6); (6) home-CLAUDE.md ladder repoint or explicit carry-over note (scope echo). W4/W7/W8 strongly recommended same-wave (small diffs); W9–W13 may ride Wave 3 with receipt notes.

WAVE VERDICT: ACCEPT_WITH_FIXES (W1 R8 ubershow evidence correction before operator decision; W2 injection citation + live probe or operator waiver; W3 standing strict drift gate; W5 gaia receipt errata; W6 R9b legal terminal state; home-CLAUDE.md carry-over note)
