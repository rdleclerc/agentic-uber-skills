# Wave 1 — Dispatch 1 of 4: R1 (unpin + lint migration) + R2 (portability)

You are Codex, the implementer for the process-rearchitecture campaign. Authority: `plan-v3.md` items R1 and R2 as amended by `round2-judgment.md` (amendments B, F, J context; A/C/D land in dispatch 3). Work ONLY inside this repo (`agentic-uber-skills`). Do NOT run any git commands — the orchestrator owns git. Do NOT touch other skills' content beyond the specified edits (no dedup — that is Wave 2). When done, print a summary: files changed, per-file rationale, word-count deltas for changed SKILL.md files, test results.

## R1 — Unpin models (lint migration, not just frontmatter)

1. `scripts/lint_pack_contract.py` (~lines 106–109) currently REQUIRES `model: claude-opus-4-8` + `effort: max` in every skill frontmatter. Flip to PROHIBITION: pack SKILL.md frontmatter must NOT contain `model:` or `effort:` keys at all, and no hardcoded model-id literal (pattern like `claude-*-N` / `gpt-N`) may appear in frontmatter.
2. Remove `model:` and `effort:` lines from all 10 SKILL.md frontmatters (uber-skill-creator, uberaccept, uberarchitect, uberassess, ubergoal, uberplan, uberrca, ubershow, ubersimplify, uberskillevolver).
3. `AGENTS.md`: delete the clause requiring the pin (line ~21). Replace with the portable lane-policy default, exactly this sentence (it will be drift-fingerprinted): "Review and acceptance lanes use the highest-capability available Claude lane; record `lane_used` in the receipt; never silently downgrade. In gaia contexts the spine's lane policy governs (see /Users/rob/repos/agfunder-gaia/knowledge/coding-agent-operating-spine.md)."
4. `README.md` (~line 9) repeats the pin requirement — update to match the new policy.
5. `ubergoal/templates/uber-run-receipt.md` (and its validator `ubergoal/scripts/validate_uber_run_receipt.py` if it checks fields): add a `lane_used` field (free text, e.g. "claude-fable-5 / gpt-5.5 xhigh"). Keep the validator change minimal: require the field present and non-empty; add/adjust fixtures under ubergoal/tests/fixtures accordingly.
6. Tests: update `tests/test_pack_contract.py` (and any per-skill tests asserting pins). Add negative fixtures BOTH ways: a fixture skill WITH a model pin must FAIL the lint; the real pack (unpinned) must PASS.

## R2 — Portability rot

1. Fix the 12 doctrine-text `/Users/claw1/` references (9 in SKILL.md files: ubergoal ×2 work-contract paths, ubershow ×6, uberskillevolver ×1; 3 in AGENTS.md: source repo, install target ×2):
   - ubergoal work-contract references → `${UBER_GUIDE_ROOT:-~/repos/agentic-architecture-guide}/docs/coding-agent-work-contract.md` and `.../.agentic/coding_agent_work_contract_template.md`, with one sentence documenting the env var + fallback.
   - ubershow claw1 output/receipt paths → the actual convention: the active task's coordination folder (`coordination/<task-slug>/`) in the repo where the work happens; delete references to `~/.openclaw/runtime/ubershow/`.
   - uberskillevolver storage path → learning records live in `learning/` of this repo (inbox/processed), packets referenced from the task's coordination folder; delete `~/.agentic-uber-learnings/` and `.uberlearn/` references.
   - AGENTS.md → source repo is "this repository's checkout location" (no absolute path); install targets `~/.codex/skills/<skill>` and `~/.claude/skills/<skill>` as symlinks to the checkout.
2. Delete the Type0 product-spine paragraphs from uberplan/SKILL.md and uberaccept/SKILL.md (project-specific content in a portable pack). Record exactly what was deleted in your summary (P1: the generic principle they encode — "prove the product spine end-to-end on real input before completion" — must already be covered by the plans' operational-outcome language; confirm it is, or keep one generic sentence).
3. Test fixtures containing claw1 paths (~13 more across ubergoal/ubersimplify/etc. fixtures + the lint constant): neutralize to `/Users/example/...` EXCEPT where the fixture explicitly tests path handling — those keep the path and gain a `# fixture-path: intentional` marker line the lint exempts.
4. NEW portability-oracle module inside `scripts/lint_pack_contract.py` (amendment J: aggregator module, not a new CLI): over doctrine text (SKILL.md, AGENTS.md, README.md, references/, templates/): (a) absolute paths must exist on this machine OR be parameterized (`${VAR:-default}` form) OR be marker-exempted; (b) machine-specific paths matching `/Users/<name>/` FAIL even if they exist, unless parameterized or marker-exempted (the `~/repos/...` and `~/.codex|~/.claude` tilde forms are fine). Negative fixtures for both rules.
5. Run everything: `python3 scripts/lint_pack_contract.py` (however it is invoked — check how tests/test_pack_contract.py calls it), `python3 -m pytest tests/ -q` or the repo's actual test entrypoint, plus each touched skill's own tests/validators. ALL green before you finish. If a rule as specified is impossible or contradicts an existing test, do the closest faithful thing and FLAG it prominently in your summary rather than silently deviating.
