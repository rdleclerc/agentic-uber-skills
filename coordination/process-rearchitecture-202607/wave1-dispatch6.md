# Wave 1 — Dispatch 6: gaia-side review fixes (F2 wiring + INDEX sync + case-22 pointer + session note)

You are Codex, implementer, working in a git WORKTREE of agfunder-gaia on branch `main` (the runtime eval layer was just re-landed here after a parallel session's reset dropped it — see finding F1 in ${UBER_SKILLS_ROOT:-~/repos/agentic-uber-skills}/coordination/process-rearchitecture-202607/wave1-adversarial-review.md). NO git commands; touch ONLY the four deliverables below.

1. **F2 — suite wiring.** New `tests/integration/failure_evals.test.mjs` following the existing tests/integration/*.test.mjs conventions (look at one for style): (a) spawns `python3 ${UBER_SKILLS_ROOT:-~/repos/agentic-uber-skills}/scripts/validate_failure_case.py evals/failures/cases` and asserts exit 0; (b) asserts INDEX.md id-set consistency: every `evals/failures/cases/*.md` id appears in `evals/failures/INDEX.md` and every runtime-canonical INDEX line has a case file, and statuses agree with frontmatter (implement the parse in JS — INDEX line format: `- <id> · <layer>(<canonical>) · <status> · ...`); (c) if the pack checkout is absent at UBER_SKILLS_ROOT, SKIP with a LOUD warning message naming the env var (do not silently pass). Keep it dependency-free (node:test, node:child_process, node:fs).
2. **gaia INDEX sync (F4 gaia half).** `evals/failures/INDEX.md`: statuses must match reality — set eval_built for claw1-path-rot, install-drift-uberarchitect, doctrine-drift-trio, credential-exposure-by-agent (their evals live in the pack); dispatch-preflight-writeability is being flipped to eval_built in the pack this wave — set it eval_built here too; all other cases seed. Pointer lines for process-canonical cases: `canonical: agentic-uber-skills/evals/failures/`.
3. **case-22 pointer.** Add one INDEX line for `parallel-branch-reset-clobbers-merge` (layer both, canonical process — canonical file lives in the pack): pointer format like the other process-canonical lines, status seed.
4. **Parallel-session note.** New file `coordination/PARALLEL-SESSION-NOTE-failure-evals-reland-2026-07-03.md` (≤20 lines): TO the session working feat/e6-auto-post-updates: your main branch-reset between 2026-07-03 ~07:00-08:00 PT dropped merge commit e0f9c17c (evals/failures runtime layer, additive); it has been re-landed on main via cherry-pick; BEFORE your next reset of main, merge current main into your branch (additive paths only — zero conflict risk: evals/failures/, scripts/run_twice_idempotency_check.sh, tests/integration/failure_evals.test.mjs, this note); the process-rearchitecture campaign's wave receipt records this as failure-catalog case parallel-branch-reset-clobbers-merge; contact surface = the campaign folder in agentic-uber-skills.

Run `node --test tests/integration/failure_evals.test.mjs` and print its output (it must pass against this tree). Print files created/changed. FLAG anything contradictory.
