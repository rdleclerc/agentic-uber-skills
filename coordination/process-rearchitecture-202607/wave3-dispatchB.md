# Wave 3 — Child dispatch B: R15 Hermes heartbeat + process telemetry (gaia worktree)

You are Codex, implementer, in a DETACHED worktree of agfunder-gaia at origin/main. NO git commands; NO launchd commands; NO live sends. Context (read `${UBER_SKILLS_ROOT:-~/repos/agentic-uber-skills}/coordination/process-rearchitecture-202607/gaia-child-plan.md` §R15 first): the memory-upgrade session already restored the pipeline (commit 04a6615b — argument-form `hermes -z`, loud-fail contract); the sole run blocker is human-owned `hermes auth` (filed, due 07-10). Your scope is ADDITIVE ONLY: heartbeat + telemetry. Touchable: `scripts/run_hermes_overseer.sh` (additive edits only — do not restructure their loud-fail design), NEW `scripts/compile_overseer_process.py`, `knowledge/hermes-overseer.md` (short doc updates), `evals/failures/cases/hermes-overseer-dead-9w.md` + INDEX.

1. **Heartbeat (case 2's eval).** run_hermes_overseer.sh writes `state/hermes-overseer-heartbeat.json` on EVERY exit path: `{started_at, finished_at, outcome: success|failed_export|failed_critique|failed_send|blocked_auth, report_path|null, error_summary|null}`. Detect the auth failure distinctly (hermes stderr contains `codex_auth_missing` or `No Codex credentials stored` → outcome=blocked_auth + error_summary naming the exact human action: "run `hermes auth` (openai-codex)"). Atomic write (tmp+mv). The existing loud-fail behavior stays untouched — heartbeat is additive.
2. **Process telemetry: NEW `scripts/compile_overseer_process.py`** writing `process.md` into the bundle dir, 5 sections, each degrading gracefully to "no data + why": (a) uber-run cost receipts — parse `${UBER_SKILLS_ROOT}/coordination/process-rearchitecture-202607/wave*-receipt.md` Run metadata (tokens/minutes/lane_used) + compare against `baseline-cost.md` (verbatim-quote the caveats line); (b) doctrine drift + install-sync — run `python3 ${UBER_SKILLS_ROOT}/scripts/lint_pack_contract.py --drift` and `--install-sync` (report-only), include output; (c) failure-DB delta — both INDEX.md files: counts by status + cases added in last 7 days (by date_observed) + seeds older than 30 days flagged as aging; (d) learning backlog — `${UBER_SKILLS_ROOT}/learning/inbox` count + oldest age + per-skill word counts vs SKILL_WORD_BUDGETS (parse the dict from lint_pack_contract.py or wc -w with budgets hardcoded-from-read); (e) skill-invocation counts — invoke the existing heuristic compiler if present (`scripts/compile_overseer_skill_usage.py`) else state absent. Wire `process.md` into the orchestrator's inlined-bundle file list (additive line in run_hermes_overseer.sh; keep prompt-size log). Secrets: none needed — everything is local file reads + the pack lint; do NOT add op:// fetches.
3. **Proof (no live send):** run `OVERSEER_DRY_RUN=1 scripts/run_hermes_overseer.sh` from the worktree root IF it is safe (read its env handling first — if the script hard-requires workspace paths, run the export + process-compile + heartbeat paths individually instead). Expected honest outcome: export ok, process.md built, critique step fails blocked_auth, heartbeat records blocked_auth. Print the heartbeat JSON + process.md head. That IS the pass state today.
4. **Case updates:** hermes-overseer-dead-9w → eval_built (eval = heartbeat + check_lane_liveness manifest entry landed by dispatch A) with a body note: pipeline restored by memory-upgrade session 04a6615b; heartbeat added by this campaign; live weekly proof pending human `hermes auth` (due 07-10). Update INDEX; run pack validator over cases + --index. `node --test tests/integration/failure_evals.test.mjs` must stay green.

Print: diffs summary, heartbeat JSON, process.md first 40 lines, all check outputs. FLAG anything that would touch the other session's design decisions instead of changing it.
