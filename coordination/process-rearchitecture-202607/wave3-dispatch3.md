# Wave 3 — Dispatch 3: acceptance conversion fixes (pack; G1-pack/G2/G3/G4/G5/G6/G8/G10/G11)

You are Codex, implementer. Pack repo only; NO git commands. Authority: wave3-adversarial-review.md. All items small; preserve budgets (ubergoal 786/800 — do not touch it).

1. **G1 pack side**: (a) INDEX.md statuses → eval_built for hermes-overseer-dead-9w, op-hang-under-launchd, pg-null-upsert-dup (match gaia); (b) extend `scripts/validate_failure_case.py --index` grammar: optional `date: YYYY-MM-DD` segment + repo-prefixed canonical dirs (`canonical: agfunder-gaia/evals/failures/` and `canonical: agentic-uber-skills/evals/failures/`) — gaia-dialect fixture must now PASS; (c) NEW cross-index mode `--cross-index <other-INDEX>`: asserts shared-id status equality between two INDEX files; wire into pack tests with the skip-loudly-if-gaia-absent pattern against `${GAIA_ROOT:-~/repos/agfunder-gaia}` origin/main content is fine via working tree read of the checkout path IF present (keep simple: read the file at GAIA_ROOT path; skip loudly if absent); divergence fixture. (d) NEW case file `evals/failures/cases/two-layer-index-status-drift.md` (layer both, canonical process, date_observed 2026-07-03, status eval_built citing the cross-index mode) + INDEX row; also append the 4th instance to unverified-baseline-claims.md (wave3-dispatch1.md:18 false "already validator-defined" claim) and a body note on evaluator-saturation.md recording G3 (replacement instrument shipped with an answer leak).
2. **G2 (ship path chosen)**: `uberaccept/scripts/validate_acceptance_report.py` + template: require `reproduced_red:` line (command/output ref or `no_repro_reason:`) whenever the report body claims a defect fix (heuristic: contains "fix" + "bug|defect|regression" — keep the heuristic conservative and documented); require `interface_shape_receipt:` (value or `n/a`) when body mentions fakes/stubs/mocks standing in for external interfaces. Negative fixtures per amendment D. NEW `scripts/validate_dispatch_ledger.py` (~40 lines): validates ledger rows (id, work_item, root, launched_at, exit, output_path, retry_count) + duplicate-cull rule (no two non-retry rows same work_item); fixtures both ways; pack test. Update cases 4/10/11/21 statuses → eval_built citing these; sync INDEX.
3. **G3**: run_routing_eval.py — strip `EXPECT-ESCALATION:` prefix + MUST-ESCALATE heading from rendered packets (keep in key/grading/--grade output); fixture asserting the R11 packet does NOT contain the marker.
4. **G4**: replace the dead upstream-freshness probe for remote-tracking refs: emit NOTE when `.git/FETCH_HEAD` mtime > 24h (guarded, silent-skip if missing); fix the fixture test to cover origin/main form.
5. **G5**: grade tier by canonical token (0|1|2|3 extracted from expected+actual) + qualifier keywords for rows with compound expectations; exact-match retired; adjust fixtures.
6. **G6**: compile_overseer_process.py — compute the comparison sentence from tokens-known count (label `static note:` gone); add the cheap deterministic cross-check (receipt minutes vs campaign-folder artifact count+bytes, clearly labeled heuristic).
7. **G8**: check_lane_liveness manifest gains openclaw-gateway (memory/gateway-canary-state.json, ttl 2h) — NOTE this file lives in the GAIA repo; make the edit as a clearly-marked patch block in your summary INSTEAD of editing (gaia file — orchestrator applies it).
8. **G10**: dispatch-and-sessions.md — one sentence separating checkpoint-commit from the preflight citation. **G11**: uberskillevolver SKILL.md gains the owner/cadence sentence ("promotion owner: operator; cadence: per-wave during campaigns, monthly otherwise"); budget 1550 holds.

Verify: full pack tests + lint --strict; both routing fixtures; validate_failure_case (cases + --index + --cross-index vs gaia if present); word deltas. Print receipts; FLAG contradictions.
