# Wave 2 — Dispatch 4b: compress openclaw-agentic-skill-creator (guide repo)

You are Codex, implementer, working in /Users/rob/repos/agentic-architecture-guide (a git repo — NO git commands; the orchestrator owns git). Target: `skills/openclaw-agentic-skill-creator/SKILL.md`, currently 5,034 words — a fork of Anthropic's skill-creator with retained banter and duplication. Compress to ≤1,500 words while keeping it the authoritative OpenClaw-specific skill creator.

KEEP (the genuinely load-bearing content): the OpenClaw-specific production contract / preamble rules that differ from the portable uber-skill-creator (tenant policy, live source lanes, agent affordance, live-safe OpenClaw proof requirements); the eval-driven iteration methodology sections (draft → subagent eval runs → eval review → iterate → description optimization) in compact form; the mutual-redirect contract with uber-skill-creator; frontmatter description UNCHANGED.

DELETE (receipt-mapped): conversational banter ("Cool? Cool.", "Good luck!", the economic-value aside, the plumbers/grandparents digression, and similar); the duplicate "Repeating one more time the core loop" recap; the stacked blockquote preamble IF its rules are restated in the body (merge unique rules into one section first); references to scripts the text itself disowns ("do not run as-is") — and decide: if run_loop.py/run_eval.py are truly disowned, note them as legacy in a one-line comment at top of each script rather than deleting files.

Method: write the compressed version; produce a deletion receipt table (removed block | words | disposition: deleted-banter / merged-into-§X / moved-to-reference) at `coordination/uberskills-w2-d4b-receipt.md` in THIS repo (create coordination/ if absent). Verify: the guide repo has no test suite for skills — run `python3 /Users/rob/repos/agentic-uber-skills/uber-skill-creator/scripts/quick_validate.py skills/openclaw-agentic-skill-creator` (read-only use of the pack validator) and report. Print before/after word counts and the receipt inline. FLAG anything OpenClaw-specific you were unsure about deleting (keep it in, flag for review).
