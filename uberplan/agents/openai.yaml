interface:
  display_name: "Uberplan"
  short_description: "Rigorous lean planning before coding"
  default_prompt: "Use $uberplan to create a lean plan contract, activate only the review lanes worth their cost, require benefit >> cost for complexity, and run a confidence gate before implementation."
policy:
  allow_implicit_invocation: true
