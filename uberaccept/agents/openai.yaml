interface:
  display_name: "Uberaccept"
  short_description: "Adversarial acceptance and proof"
  default_prompt: "Use $uberaccept only when explicitly invoked or routed by $ubergoal; do not auto-trigger from task similarity; perform final adversarial acceptance, verify risk-mapped evidence, map every micro-intent/work-contract/PRD/plan acceptance criterion to pass/fail/partial evidence, check user expectation/surprise delta, check architecture and Agent Advocate concerns, and require benefit >> cost before recommending completion."
policy:
  allow_implicit_invocation: true
