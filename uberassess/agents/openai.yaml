interface:
  display_name: "Uberassess"
  short_description: "Assess external sources before adoption"
  default_prompt: "Use $uberassess only when explicitly invoked, when I ask to assess an external source for adoption, or when routed by $ubergoal; produce a source-grounded recommendation packet, enforce benefit >> cost, keep implementation behind approval, and hand approved work to $ubergoal/$uberplan."
policy:
  allow_implicit_invocation: false
