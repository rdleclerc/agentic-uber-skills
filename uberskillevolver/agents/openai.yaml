interface:
  display_name: "Uberskillevolver"
  short_description: "Evolve skills from evidence without drift"
  default_prompt: "Use $uberskillevolver only when explicitly invoked or routed by $ubergoal; do not auto-trigger from task similarity; after substantial Tier 2/3 skill, prompt, workflow, or agentic-system work; capture lessons, decide whether repeated AI-code failure patterns belong in a lightweight slop register, and decide which lessons should become evals, validators, template changes, or no change."
policy:
  allow_implicit_invocation: true
